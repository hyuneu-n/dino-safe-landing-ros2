# Connected Metropolis — 도시 환경

2026-09-14. 현재 `run_city_demo.sh`가 기본으로 여는 월드.

뉴욕풍 고층 도심과 벽돌 상가 → 공장·물류 지구 → 저층 주거 타운을 하나의 도로망으로 연결했다. 남쪽에는 수변 산책로, 주거지 뒤에는 텃밭, 동쪽에는 공사장이 있다. 구역만 커지는 대신 **12m 하강 카메라에 서로 다른 표면과 장애물이 실제로 보이는 것**을 기준으로 목적지를 구성했다.

## 실행

```bash
# 환경만 둘러보기: 탐지기와 미션을 시작하지 않음
bash run_city_demo.sh 0 world

# 기존 탐지기·미션·RViz와 함께 실행
bash run_city_demo.sh 0

# RViz 없이 전체 미션
bash run_city_demo.sh 0 norviz
```

생성기만 실행:

```bash
python3 worlds/generate_metropolis.py --seed 0 --out /tmp/metropolis/city.world
```

`.world`, `.waypoints.json`, `city_assets/` COLLADA 메시 디렉터리가 함께 생성된다. 월드를 다른 위치/컴퓨터로 옮길 때에는 그곳에서 재생성한다(월드의 메시 경로는 절대 경로). 별도 에셋 다운로드 없이 Python 표준 라이브러리로 생성한다. 시드는 건물 높이·재질·주택 지붕을 바꾸고, 거리와 목적지 위치는 유지한다.

기존 `worlds/generate_city.py`는 이전 격자·맨해튼·마을 생성기로 보존했다. 기존 비교실험 결과도 이전 평가 환경의 수치이며, 새 도시에서의 성능 수치가 아니다.

## 시각 구성

- 도심: 유리 타워, 석재 랜드마크, 벽돌 중층 건물, 상가 차양, 창틀, 외부 비상계단, 옥상 설비, 횡단보도와 신호등.
- 산업: 창고·하역문·도크·채광창, 컨테이너, 트럭, 팔레트, 울타리, 굴뚝.
- 주거: 경사지붕 주택, 정원과 담장, 주차 차량, 마을 공원, 상점 주차장, 텃밭.
- 수변·공사: 산책로, 벤치와 수목, 난간, 수면, 비계, 낮은 건축자재.

건물 63동. 작은 창문·창틀 등을 각각 Gazebo 모델로 만들지 않고 건물별 COLLADA 메시로 묶는다. 건물 본체의 충돌 크기는 시각 메시와 같은 치수에서 생성한다. 나무 수관·경사지붕 등은 보수적인 박스 충돌 근사이며, 창문과 난간 일부는 시각 디테일이다. 정적 사람 모델과 기존 `person_intruder` 데모도 포함한다.

## 착륙 상황 12종

| 목적지 | 좌표 x, y | 카메라에서 확인할 조건 |
|---|---|---|
| Hudson Green | -113, 22 | 잔디·산책로, 나무·벤치·화단 |
| Civic Plaza | 60, 25 | 석재 광장, **지정 좌표의 사람**, 주변 빈 공간 |
| Lexington Ave | 30, -28 | 빌딩 협곡, 주차 차량과 교통 볼라드 |
| East Gate | 150, 0 | 기본 미션 종점, 아스팔트·화단·도로 표식 |
| Foundry Yard | 244, -79 | 컨테이너·팔레트·트럭, **지정 좌표의 화물** |
| Warehouse Court | 332, 42 | 창고 앞 하역장, 낮은 팔레트와 높은 적재물 |
| Maple Green | 490, 23 | 마을 잔디 공원, 수목·벤치·보행자 |
| Maple Market | 611, -23 | 주차선과 차량, **지정 좌표에도 차량** |
| Oak Lane | 520, 28 | 담장·정원 사이 골목과 주차 차량 |
| Allotment Gate | 580, 148 | 흙·작물·높인 화단·나무 울타리 |
| Riverside Works | 675, 5 | 비계와 건축자재, **지정 좌표의 낮은 벽돌 더미** |
| Riverwalk | 90, -160 | 수변의 길쭉한 포장면, 벤치·화단·수목 |

4곳은 목적지 좌표가 점유되어 있어 주변으로 착륙점을 옮겨야 한다. 모든 시나리오에는 스캔 범위 안에 4×4m의 장애물 없는 대체 공간을 설계하고 정적 충돌 검사로 확인한다. 이 검사 좌표는 생성기 메모리에만 있으며, 탐지기·미션 컨트롤러에 전달하지 않는다. `.waypoints.json`에는 표시용 목적지, 구역 경계, 건물 외곽, 장애물 종류, `nominal_target_blocked`를 제공한다. 비전 노드에는 변경이 없다.

## 검증 및 한계

```bash
python3 -m unittest discover -s eval -p test_metropolis.py
bash eval/validate_metropolis.sh /tmp/metropolis_validation
```

첫 검사는 시드 0·7·23의 건물 겹침, 모델 이름 중복, 메시 파일 참조, 대체 공간의 충돌 여부를 검사한다. 두 번째는 별도 Gazebo master/ROS domain에서 서버를 띄워 구역 전경 5장과 목적지 12곳의 RGB·depth를 캡처하고 서버를 종료한다. `review/sensor_report.json`에 유효 depth 비율과 깊이 범위를 저장하며, 시야에 장애물이 없어 깊이 변화가 거의 없는 경우 실패한다.

이번 환경 작업은 렌더링과 센서·배치 검증까지다. **새 도시에서 DINOv2의 성공률이나 12개 목적지의 비행·착륙 성공을 검증한 것은 아니다.** 기존 제어기는 순항 중 클릭 시 직행으로 바꾸며 전체 도시 도로망을 재탐색하지 않는다. 착륙 후 재출발도 별도 구현이 필요하다. 옥상 착륙은 기존 고도 처리부터 고쳐야 하므로 이번 목적지는 지상으로 한정했다. 물은 정적 시각 표면으로, 유체 시뮬레이션은 아니다.

## 다음 단계: 참여형 시연과 대시보드

환경 다음 순서로 진행할 후보이며, 아직 구현하지 않았다.

1. 구역 지도와 목적지 카드: 도심/공장/주거/수변을 골라 배송 요청, 해당 장소의 카메라로 전환.
2. 반복 배송: 착륙 후 새 목적지 선택·재이륙, 목적지의 높이와 경로를 일관되게 처리.
3. 장애물 개입: 사람 이동, 차량 추가, 팔레트 배치. 명령은 환경만 바꾸고 탐지기는 카메라로 변화 감지.
4. 상황 프리셋: 낮은 장애물/좁은 공간/움직이는 사람/표면 혼합을 선택해 비교.
5. 근거 표시: RGB·depth·안전 마스크, 후보점과 최종 선택점, 현재 비행 상태를 한 화면에 표시.
6. 재현: 시드·목적지·장애물 변경 이력과 결과를 저장해서 같은 조건을 다시 실행.
